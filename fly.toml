app = 'daemon'
primary_region = 'ams'

[mounts]
  destination = "/data"
  source = "data"

[build]

[[services]]
  protocol = 'tcp'
  internal_port = 2375
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
